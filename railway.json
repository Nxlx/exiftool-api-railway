{
    "build": {
      "commands": [
        "apt-get update && apt-get install -y exiftool",
        "pip install -r requirements.txt"
      ]
    },
    "start": "gunicorn app:app",
    "healthcheckPath": "/"
  }
  